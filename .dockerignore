# VCS / editor
.github
.git
.vscode
.gitignore
.idea
.zed

# Markdown / docs (not needed in image)
*.md
docs

# Build / cache artifacts
.eslintcache
.gocache
.gocache-temp
.gomodcache
.gopath

# Frontend node_modules (copied per-stage by builder, not needed in build context)
# IMPORTANT: list each path explicitly. A bare `node_modules` won't match nested dirs.
web/node_modules
web/default/node_modules
web/classic/node_modules

# Frontend dist (regenerated by builder stages 1 & 2)
web/default/dist
web/classic/dist
web/default/.cache
web/classic/.cache
web/dist

# Runtime / dev
data
logs
*.log
upload
.env
.env.local
.env.local.example
docker-compose.local.yml
docker-compose.dev.yml

# Misc
Makefile
electron
plans
.claude
.cursor
skills-lock.json
tools
