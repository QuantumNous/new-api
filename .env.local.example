# 本地开发环境模板（macOS + SQLite）
#
# 使用方式：
#   cp .env.local.example .env.local
#   go run main.go
#
# 说明：
#   .env.local 优先于 .env 加载，本地覆盖值写这里，不影响其他环境
#   SQLite 数据库文件自动创建在 ./data/one-api.db，无需额外配置
#   .env.local 已加入 .gitignore，不会提交到仓库

# KYC 加密 key（AES-256-GCM）— 固定 dev 值，重启后数据不失效
# 生产环境必须用 openssl rand -hex 32 独立生成，禁止复用此值
KYC_ENCRYPT_KEY=00000000000000000000000000000000000000000000000000000000deadbeef
KYC_HASH_KEY=00000000000000000000000000000000000000000000000000000000cafebabe

# 其他本地覆盖（按需取消注释）
# PORT=3000
# SESSION_SECRET=dev-session-secret
# DEBUG=true
