# Disabled upstream workflows

These workflow definitions are retained for upstream reference but are intentionally stored outside `.github/workflows`, so GitHub Actions does not execute them in this maintenance fork.

The only active workflow is `.github/workflows/build-maintenance-image.yml`, which publishes the fork's multi-architecture maintenance image to GHCR.
