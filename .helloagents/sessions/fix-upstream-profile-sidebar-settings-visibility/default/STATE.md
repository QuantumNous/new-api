# 恢复快照

## 主线目标
调整 aiapi114 模型状态页设计：用户侧状态监控页面不得展示具体渠道、上游供应商或供应商状态接口名称，只展示分组以及下属模型监控信息。

## 正在做什么
已完成设计文档调整和格式检查，准备提交并汇报。

## 关键上下文
- 设计文档位置：`docs/channel/model-status-page-design.md`。
- 已将页面结构从“供应商 + 分组 + 模型”调整为“分组 + 模型”。
- 已移除供应商筛选项、供应商分区、供应商统计项和公开状态页中的供应商展示要求。
- 已补充约束：供应商 / 渠道 / 上游名称只作为后端同步和内部诊断字段，不能出现在用户界面、筛选、说明文案、DOM 可访问名称和公开页面 URL 中。
- 已明确 `categoryName` 只用于内部诊断和去重，前端不渲染。
- 已定义多个来源返回相同 `group + model` 时的前端聚合规则：只展示一行，状态按最差状态聚合。

## 下一步
提交文档调整，并向用户说明已更新的展示约束和文档位置。

## 阻塞项
无。
