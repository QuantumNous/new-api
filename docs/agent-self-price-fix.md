# 代理本人计价修复

2026-09-07 随 `hardy-balance-self-20260907-184205` 上线。

问题：启用代理并配置 0.02 元后，只有存在邀请价格绑定的客户会取得代理报价；没有邀请人的代理本人仍按平台 0.06 元计价。

修复：有效代理本人优先使用自己配置中的 `gpt-image-2` 价格。停用时回到原有客户绑定或平台规则。客户按邀请链接锁定的价格继续独立保存。本人报价同样参与网页批次锁价、API 预扣与结算；不调整已提交批次的锁定快照。管理员界面增加该价格适用于代理本人的说明。

验证：先编写能复现本人报价缺失的失败用例，再验证本人报价、停用/禁用回退及现有代理邀请和结算测试。生产 PostgreSQL 的隔离还原中，使用 bang 账号数据（无邀请人）验证报价为 2 分；API 两张模拟图片扣 0.04 元，网页批量两张模拟图片也扣 0.04 元。隔离网络禁止访问真实上游，测试未产生收费调用。

上线后，bang 的代理仍启用、配置单价 2 分、版本 2。旧页面应刷新后再提交新任务。

历史审计：截至上线检查，日志 212–221、224、226–233 共 19 张按 6 分扣费，原应按 2 分；差额 380000 quota，即 0.76 元。用户随后取消补款，未改变原消费日志或余额。服务器审计记录：`/opt/new-api-releases/20260907-184205/bang-billing-review.json`。
