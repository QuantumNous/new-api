# AI 任务模板（Codex / Claude）

> 使用说明：所有 AI 二开任务在开始改代码前，必须先按本模板填写；未填写完整前，不允许直接进入实现。

这样做符合 `KISS` 和 `YAGNI`，直接收益是任务范围、验证方式和风险边界在开始前就被锁定，减少“自由发挥”式改动。

## 1. 任务目标

- 要解决的问题：
- 业务目标：
- 预期交付：

## 2. 成功标准

- 功能或行为层面的成功标准：
- 技术层面的成功标准：
- 如何判断本次任务已经完成：

## 3. 本次范围

- 本次会修改的模块 / 目录：
- 本次明确不做什么：
- 是否允许顺手重构：默认不允许，除非与当前任务强相关且能说明收益

## 4. 影响层次判断

请逐项回答“是 / 否”，若为“是”则补充验证要点：

- 是否涉及架构层调整：
- 是否涉及数据库层 / model / migration：
- 是否涉及 DTO / relay 请求字段：
- 是否涉及前端用户可见文案：
- 是否涉及 Docker / Compose / `.env.example`：
- 是否涉及同步官方 upstream：

## 5. 风险与兼容性

- 如果涉及数据库，如何保证 SQLite / MySQL / PostgreSQL 同时兼容：
- 如果涉及 relay DTO，如何保证显式零值不丢失：
- 如果涉及前端文案，是否需要同步 i18n：
- 是否存在需要特别关注的回归点：

## 6. 实施方案

- 拟修改的核心模块：
- 每一步为什么这样做：
- 这一步分别对应哪些原则：`KISS / YAGNI / DRY / SOLID`
- 预期直接收益：

## 7. 必跑验证

- 后端验证：
- 前端验证：
- i18n 验证：
- Docker / Compose 验证：
- 其他手动验证：

## 8. 必须遵守的项目规则

执行前必须再次确认：

- 必须遵守 `AGENTS.md`
- 禁止业务代码直接使用 `encoding/json`
- 前端统一使用 `bun`
- 数据库改动必须兼容 SQLite / MySQL / PostgreSQL
- 受保护项目标识不得修改
- 所有改动文件必须为 UTF-8（无 BOM）

## 9. 完成后汇报模板

- 完成的核心任务：
- 原则落地说明：
- 验证结果：
- 风险与未覆盖点：
- 下一步建议：
