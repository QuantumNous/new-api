VITE_API_TARGET=http://localhost:3000
VITE_DEV_PORT=5175
VITE_TRUSTED_EXTERNAL_ORIGINS=
# Console/auth transport: production defaults to "http". Set "mock" only for
# local development, unit tests and explicitly read-only prototypes.
VITE_API_MODE=mock
# Optional public-page override; defaults to VITE_API_MODE.
VITE_PUBLIC_API_MODE=
